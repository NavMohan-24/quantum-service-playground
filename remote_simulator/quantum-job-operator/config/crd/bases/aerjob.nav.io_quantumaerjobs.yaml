---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: quantumaerjobs.aerjob.nav.io
spec:
  group: aerjob.nav.io
  names:
    kind: QuantumAerJob
    listKind: QuantumAerJobList
    plural: quantumaerjobs
    singular: quantumaerjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.backendName
      name: Backend
      type: string
    - jsonPath: .status.retries
      name: Retries
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        description: |-
          Setting status of a subresource implies it could be accessed via quantumaerjob/status.
          QuantumAerJob is the Schema for the quantumaerjobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of QuantumAerJob
            properties:
              backendName:
                default: aer-simulator
                description: BackendName is the Qiskit Aer backend to use
                type: string
              circuits:
                description: |-
                  Circuits is a string representation of the quantum circuits to execute.
                  This should be a base64-encoded QPY serialized circuit.
                type: string
              jobID:
                description: JobID is a unique identifier for this simulation job.
                type: string
              maxRetries:
                default: 3
                description: MaxRetries allowed if pod fails
                format: int32
                type: integer
              resources:
                description: Resources defines the compute resources required for
                  the simulator pod
                properties:
                  limits:
                    description: Limits describes the maximum amount of compute resources
                      allowed
                    properties:
                      cpu:
                        default: 500m
                        description: CPU in cores (e.g., "500m" for 0.5 cores)
                        type: string
                      memory:
                        default: 512Mi
                        description: Memory in bytes (e.g., "512Mi")
                        type: string
                    type: object
                  requests:
                    description: Requests describes the minimum amount of compute
                      resources required
                    properties:
                      cpu:
                        default: 500m
                        description: CPU in cores (e.g., "500m" for 0.5 cores)
                        type: string
                      memory:
                        default: 512Mi
                        description: Memory in bytes (e.g., "512Mi")
                        type: string
                    type: object
                type: object
              shots:
                default: 1024
                description: Shots is the number of times to run the circuit.
                format: int32
                minimum: 1
                type: integer
              simulatorImage:
                description: SimulatorImage contains the image for simulator
                type: string
              timeOut:
                default: 600
                description: Timeout for the simulation job in seconds
                format: int32
                type: integer
              ttlSecondsAfterFinished:
                default: 300
                description: |-
                  TTLSecondsAfterFinished limits the lifetime of a Job that has finished
                  execution (either Completed or Failed).
                format: int32
                type: integer
            required:
            - circuits
            - simulatorImage
            type: object
          status:
            description: status defines the observed state of QuantumAerJob
            properties:
              completionTime:
                description: CompletionTime is when the job finished
                format: date-time
                type: string
              conditions:
                description: The status of each condition is one of True, False, or
                  Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              errorMessage:
                description: ErrorMessage contains error details if the job failed
                type: string
              podName:
                description: PodName is the name of the simulator pod
                type: string
              result:
                description: Result is the base64-encoded execution result
                type: string
              retries:
                default: 0
                description: Retries tracks the number of retry attempts
                format: int32
                type: integer
              startTime:
                description: StartTime is when the job started executing
                format: date-time
                type: string
              state:
                description: JobStatus is the current state of the Job
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
